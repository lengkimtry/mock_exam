# Disable package-lock.json during docker builds for speed
package-lock=false

# Use faster registry
registry=https://registry.npmjs.org/

# Disable audit and funding messages
audit=false
fund=false

# Disable optional dependencies
optional=false

# Use cache more aggressively
cache-min=86400

# Disable progress bars
progress=false

# Disable timing information
timing=false
